services:
  smtp:
    container_name: "smtp"
    image: "mwader/postfix-relay"
    volumes:
      - "./smtp:/etc/opendkim/keys"
    environment:
#      - "POSTFIX_myhostname=DOMAIN.TEST"
#      - "OPENDKIM_DOMAINS=DOMAIN1.TEST=example-mail DOMAIN2.TEST=example-mail"
      - "POSTFIX_myhostname=example.test"
      - "OPENDKIM_DOMAINS=example.test=example-mail"
      - "TZ=Europe/Berlin"
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
    restart: "unless-stopped"
    networks:
      - "smtp"

networks:
  smtp:
    name: "smtp"
    driver: "bridge"
